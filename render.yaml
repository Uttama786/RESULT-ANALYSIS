services:
  - type: web
    name: vtu-result-scraper
    env: docker
    plan: free
    dockerCommand: "uvicorn backend.app:app --host 0.0.0.0 --port ${PORT:-8000}"
    envVars:
      - key: PORT
        value: 8000
